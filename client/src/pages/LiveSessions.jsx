/**
 * client/src/pages/LiveSessions.jsx
 * Renders the CoursePlayer for the most-recent in-progress enrolment.
 */
import CoursePlayer from './CoursePlayer';

export default function LiveSessions() {
  return <CoursePlayer />;
}
